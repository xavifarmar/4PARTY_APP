<!-- res/drawable/border.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/background"/> <!-- Fondo blanco, puedes cambiarlo -->
    <stroke
        android:color="@color/text"
        android:width="0.5dp"/>  <!-- Grosor del borde -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp"/>  <!-- Espaciado interno -->
</shape>
